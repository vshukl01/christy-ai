/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // ✅ remove invalid option you saw in logs
  // experimental: { appDir: true },  // <-- DELETE this if you had it
};

export default nextConfig;